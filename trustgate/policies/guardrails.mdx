---
title: "Guardrails"
description: "Inspect what goes in and what comes out. TrustGuard, a provider's moderation service, an AWS or Google guardrail, or deterministic regex — and the one thing none of them can do."
---

Applies to **LLM and MCP** — the Model Context Protocol your agents call tools
over. Each guardrail inspects the request, the response, or both, and blocks or
transforms what matches.

## The one limit shared by all of them

**Streaming responses cannot be inspected or blocked in real time.** Tokens are
already leaving when a detector would decide. Put guardrails on the request leg,
or on non-streaming responses, for enforcement. A response guardrail on a
streaming application observes; it does not protect.

## TrustGuard

Runs [TrustGuard](/trustguard/overview) detectors — jailbreak, PII, secrets,
toxicity, tool abuse, multi-turn attacks — against the request, the response, or
both, and applies the action TrustGuard returns. It needs an Agent Runtime
collector connected to this gateway, and **it does not create one for you** — the
policy will not save until one exists.

**On error**: fail open for transport and service failures. Authentication,
rate-limit and entitlement responses from TrustGuard refuse the request
regardless, **and in Observe too** — a gateway that cannot prove it is allowed to
call the detector does not get to guess. The
[TrustGuard page](/trustgate/policies/trustguard-guardrail#modes-and-failures)
has the full table.

Findings correlate with [Telemetry alerts](/platform/alerts), which is the
argument for this one over a provider's moderation API when you already run
TrustGuard.

## OpenAI Moderation

Screens text with OpenAI's moderation model against per-category thresholds; a
score at or above a threshold blocks, and **block on flagged** blocks anything
OpenAI flags even without a threshold. Needs an OpenAI key of its own. Text only.

In enforce mode it **fails closed** on any moderator error. Note before you pick
it: neither the thresholds nor **block on flagged** has a field on its form, so
a policy you create in the console scores everything and refuses nothing until
someone sets one of them for you.

## Azure Content Safety

Azure's Analyze Text against **Hate**, **Violence**, **SelfHarm** and **Sexual**,
with a severity threshold per category; only the categories you list are
enforced. Needs the endpoint and a subscription key. Request leg only. Fails
closed in enforce mode.

## AWS Bedrock guardrail

Reuses a guardrail you already defined in Bedrock — its topic, content, word,
sensitive-information and grounding policies — against prompts and responses.
Authenticate with static keys or an IAM role. The one distinctive option is
**PII action**: block, or **anonymise in place** — redacting rather than refusing,
which only this and Google Model Armor can do.

## Google Model Armor

Reuses a template you defined in Google Cloud, covering sensitive data,
Responsible AI categories, prompt injection and jailbreak, malicious URLs and
CSAM in a single call against prompts and responses. Like Bedrock it can
**mask in place** instead of refusing, reinjecting the de-identified text Google
returns.

Standing it up is more work than the others: the template depends on two
Sensitive Data Protection templates, a region that must match everywhere, and an
IAM grant that Editor does not include. It has a guide of its own.

<Card title="Set up Google Model Armor" icon="shield-check" href="/trustgate/policies/google-model-armor">
Google Cloud template, credentials, policy settings, and how to verify it.
</Card>

## Regex Replace

Ordered RE2 rewrite rules on the request **or** the response — one leg per
policy. Each rule sees the previous rule's output. No external service, fully
deterministic, and it does exactly what the pattern says and nothing more, which
is both its virtue and its limit.

## Combining them

A chain may hold several. TrustGuard on the request and a Bedrock guardrail
anonymising PII on the response is a common pair: the first refuses what should
not go out, the second cleans what comes back.
