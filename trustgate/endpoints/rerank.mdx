---
title: "Rerank"
description: "Call POST /{application}/v1/rerank with a Cohere v2 body. TrustGate routes only to registries that can rerank."
---

Clients send the same JSON they would send to Cohere `/v2/rerank`. Auth and the
application slug are the same as [chat](/trustgate/getting-started/quickstart#call-from-your-application).

```bash
curl -X POST "https://<gateway-host>/<application-slug>/v1/rerank" \
  -H "X-AG-API-Key: <your-api-key>" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "rerank-english-v3.0",
    "query": "What is TrustGate?",
    "documents": ["TrustGate is an LLM gateway", "Unrelated text"]
  }'
```

On a Private data plane, add `X-AG-Gateway-Slug` as in the chat snippets.

The gateway speaks the Cohere v2 rerank shape on this path. It is not an OpenAI Chat
Completions route.

## Providers

| Registry | Upstream |
|---|---|
| **Cohere** | Cohere `/v2/rerank` |

OpenAI, Azure, Anthropic, Gemini / Vertex, Bedrock, Groq, Mistral, OpenRouter, and other
chat-only providers do not expose rerank. They are left out of the candidate pool.

The `model` field is allowlisted as usual and can pin with `@provider/model`.

## Routing

1. The application's registries are filtered to those that advertise rerank.
2. [Model resolution](/trustgate/llm/routing#what-the-client-sends) and
   [load balancing](/trustgate/llm/routing#load-balancing) run on that pool.
3. Pinning a registry that cannot rerank is a **400**, not a failover.
4. An empty capable pool is a **503**.

Attach at least one Cohere registry to the application. A mixed chat + rerank pool is fine:
chat still uses every chat-capable member; rerank skips the rest.

## Related

- [Providers](/trustgate/concepts/providers) — rerank is Cohere only
- [Registries](/trustgate/concepts/registries)
- [Applications](/trustgate/access/applications) — Connect tab
- [Embeddings](/trustgate/endpoints/embeddings)
- [Models](/trustgate/endpoints/models)
- [Quickstart](/trustgate/getting-started/quickstart)
