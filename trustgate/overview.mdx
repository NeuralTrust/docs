---
title: TrustGate
sidebarTitle: Overview
og:title: "TrustGate: AI Gateway for LLM and Agent Traffic"
twitter:title: "TrustGate: AI Gateway for LLM and Agent Traffic"
description: "TrustGate is an AI agent gateway that routes, governs, and observes traffic from agents to models, tools, and MCP servers."
---

<p className="nt-lead">
  TrustGate is an AI agent gateway: a control plane between agents and the
  models, tools, and MCP servers they call. Point an OpenAI, Anthropic, or
  Responses client at it. Then route, authenticate, apply policy, and record
  every call — from the NeuralTrust console or the control-plane API.
</p>

## How it works

<Steps>
<Step title="Authenticate the caller">
A [consumer](/trustgate/concepts/consumers) presents an API key, or a JWT
that TrustGate validates against the user's IdP. The application never holds
the provider credential.
</Step>
<Step title="Apply policy and route">
TrustGate evaluates [rate limits, budgets, tool allow-lists, and
guardrails](/trustgate/policies/overview), then the consumer's
[registries](/trustgate/concepts/registries) handle the upstream, with
[fallback](/trustgate/routing/fallback) and
[smart routing](/trustgate/routing/smart-routing) when you configure them.
</Step>
<Step title="Record the hop">
Tokens, cost, latency, and routing attempts go to traces and
[OpenTelemetry](/trustgate/observability/telemetry).
</Step>
</Steps>

<CardGroup cols={2}>
  <Card title="Create a gateway" icon="rocket" href="/trustgate/getting-started/quickstart">
    Register a provider, create a consumer, and send the first request.
  </Card>
  <Card title="Install a data plane" icon="server" href="/trustgate/getting-started/install">
    Run TrustGate in your environment when prompts cannot leave your network.
  </Card>
</CardGroup>

<img
  src="/images/static/img/trustgate-product.svg"
  alt="Before-and-after diagram showing separate controls in each application consolidated into TrustGate, which applies shared controls and routes several applications to multiple LLMs."
/>

## What you can do

<CardGroup cols={2}>
  <Card title="Route across providers" icon="route" href="/trustgate/routing/smart-routing">
    Fallback chains, load balancing, and smart routing by request complexity.
  </Card>
  <Card title="Govern traffic" icon="shield-check" href="/trustgate/policies/overview">
    Rate limits, LLM budgets, tool allow-lists, prompt controls, and guardrails.
  </Card>
  <Card title="Speak one API" icon="plug" href="/trustgate/endpoints/chat">
    Chat, embeddings, images, files, audio, rerank, and model discovery.
  </Card>
  <Card title="See supported providers" icon="boxes" href="/trustgate/concepts/providers">
    Every adapter TrustGate can route to, and which modalities it supports.
  </Card>
  <Card title="Expose MCP tools" icon="bot" href="/trustgate/mcp/overview">
    One MCP gateway for Claude Code, Cursor, Codex, Copilot, and Copilot Studio.
  </Card>
  <Card title="Authenticate consumers" icon="key-round" href="/trustgate/concepts/auth">
    API keys, or JWTs validated against your IdP.
  </Card>
</CardGroup>

Attach [TrustGuard](/trustguard/overview) when you want inline inspection of
prompts, responses, and tool activity. See the
[integrations catalog](/integrations/overview) for coding agents and other gateways.

## Core objects

Everything in the console under **TrustGate** is one of these:

| Object | What it is |
|---|---|
| **[Gateway](/trustgate/concepts/gateways)** | The top-level tenant. Owns everything below. |
| **[Registry](/trustgate/concepts/registries)** | An upstream backend: an LLM provider or an MCP server. |
| **[Consumer](/trustgate/concepts/consumers)** | The calling application's identity. Owns routing and credentials. |
| **[Auth](/trustgate/concepts/auth)** | An API key, or a JWT TrustGate validates against the consumer's IdP. |
| **[Policy](/trustgate/policies/overview)** | Rate limits, budgets, tool governance, guardrails, and other controls. |
| **[Role](/trustgate/concepts/roles)** | Routing selected from OIDC token claims. |

## Request path

```text
client ──▶ /{consumer_slug}/v1/chat/completions
              │  X-AG-API-Key
              │  X-AG-Gateway-Slug (Private only)
              ├─ resolve gateway + consumer + policies
              ├─ apply policies (rate limit, budgets, guardrails, …)
              ├─ route across the consumer's registries (+ fallback)
              ├─ forward to the provider adapter
              └─ export telemetry → OpenTelemetry
```

The client names a **model** or uses `auto`. It does not send a provider URL or
key. [Architecture](/trustgate/architecture) covers the full lifecycle.

## FAQ

<AccordionGroup>
<Accordion title="Can I move an existing OpenAI or Anthropic client?">
Yes. Change the base URL and replace the provider credential with a TrustGate
consumer credential. TrustGate accepts OpenAI Chat Completions, Responses,
Anthropic Messages, and Google Gemini formats, then adapts the request for the
selected upstream. Start from the connection snippet on the consumer **Connect**
tab.
</Accordion>
<Accordion title="API key or JWT?">
Use an API key for a static application. Or send a JWT — TrustGate validates
it against your IdP and can route from token claims. See
[Authentication](/trustgate/concepts/auth).
</Accordion>
<Accordion title="Can TrustGate run in my environment?">
Yes. Hybrid keeps the data plane in your network. External runs the full
platform yourself. SaaS hosts both planes. Compare models in
[Deployment](/neuraltrust/deployment/overview).
</Accordion>
<Accordion title="Do applications need the provider's credentials?">
No. Provider credentials stay on the registry. Applications authenticate as a
consumer. Rotate consumer keys separately from provider keys.
</Accordion>
</AccordionGroup>
