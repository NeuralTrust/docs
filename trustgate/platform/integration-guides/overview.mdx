---
title: Integration guides
description: Apply Runtime security to your agent stack — SDKs, frameworks, managed agent platforms, and developer tools.
---

Runtime by TrustGate can secure any LLM or agent traffic through one of four [enforcement surfaces](/trustgate/platform/enforcement-surfaces). These guides show how to wire it up for the most common stacks.

Each guide follows the same structure:

- **What this covers** — which surfaces apply and why
- **Architecture** — where TrustGate sits in the call path
- **Prerequisites** — what you need to create in the platform first
- **Wire it up** — minimal code or configuration change
- **Verify** — confirm traffic is flowing
- **Policies to turn on first** — recommended starting detectors
- **Limitations** — known caveats

## Homegrown & frameworks

You control the LLM call. Integration is usually a base-URL and API-key swap to a [Gateway](/trustgate/platform/enforcement/gateway).

<CardGroup cols={2}>
  <Card title="LLM SDKs" icon="code" href="/trustgate/platform/integration-guides/openai-sdk">
    Route direct calls from the OpenAI, Anthropic, Google, Azure OpenAI, and Bedrock SDKs through a Gateway.
  </Card>
  <Card title="LangChain & LangGraph" icon="link" href="/trustgate/platform/integration-guides/langchain">
    Point `ChatOpenAI` / `ChatAnthropic` clients at a Gateway and reuse your existing chains and graphs.
  </Card>
  <Card title="Strands Agents SDK" icon="git-branch" href="/trustgate/platform/integration-guides/strands-agents">
    Protect every model hop in AWS's open-source agent SDK — one swap on the model client covers the whole agentic loop.
  </Card>
</CardGroup>

## Managed agent platforms

The vendor hosts orchestration. Integration depends on whether you control the model call and how the agent is exposed to consumers.

<CardGroup cols={2}>
  <Card title="AWS Bedrock Agents" icon="cloud" href="/trustgate/platform/integration-guides/bedrock-agents">
    Two options: Strands + [Gateway](/trustgate/platform/enforcement/gateway) for agents you build with the Strands SDK, or [API surface](/trustgate/platform/enforcement/api) pre/post for the managed Bedrock Agents service.
  </Card>
  <Card title="Azure AI Foundry" icon="cloud" href="/trustgate/platform/integration-guides/azure-foundry">
    Route a published Foundry Agent Application through a Gateway, with Entra authentication handled at the edge.
  </Card>
</CardGroup>

## Developer & productivity tools

You don't control the LLM call — the tool is a SaaS IDE or chat app that ships its own networking. Enforcement happens at the user's browser or device via the [Browser](/trustgate/platform/enforcement/browser) and [Endpoint](/trustgate/platform/enforcement/endpoint) surfaces.

<CardGroup cols={2}>
  <Card title="Cursor, Copilot & AI IDEs" icon="code" href="/trustgate/platform/integration-guides/cursor">
    Protect Cursor, GitHub Copilot, Windsurf, and similar AI IDEs with Endpoint (native traffic) and Browser (web UIs).
  </Card>
</CardGroup>

## Don't see your stack?

Any HTTP-based LLM or agent traffic can be wrapped with Runtime. If your platform isn't covered here, pick the surface that matches your integration point:

- You control the LLM call → [Gateway](/trustgate/platform/enforcement/gateway)
- You expose an agent through your own API → [API](/trustgate/platform/enforcement/api)
- The agent is a web app your users open in a browser → [Browser](/trustgate/platform/enforcement/browser)
- The agent is a native desktop app, IDE plugin, or CLI on a managed device → [Endpoint](/trustgate/platform/enforcement/endpoint)

Still unsure? Contact your account team or email [support@neuraltrust.ai](mailto:support@neuraltrust.ai).
