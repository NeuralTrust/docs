# Allow fake secret examples in docs that intentionally demonstrate masking targets.
allow-rules:
  - id: trustgate-data-masking-entities-docs
    description: Ignore example secret-like values in the masking entities reference page.
    path: ^trustgate/data-masking/entities\.mdx$
