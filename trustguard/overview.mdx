---
title: "Overview"
description: "TrustGuard inspects LLM and agent traffic against a policy and returns a verdict for the collector to apply"
---

**TrustGuard** is NeuralTrust's runtime security engine. It inspects prompts, completions,
documents, URLs, and tool calls, then returns a structured verdict. It is **not** a
gateway: it does not route, load-balance, or talk to model providers. The
[collector](/trustguard/concepts/collectors) that called it decides whether to allow,
mask, prompt the user, or block.

It sits next to **[TrustGate](/trustgate/overview)** (native inline enforcement) and
**[TrustTest](/trusttest/getting-started/overview)** (red teaming). You can also attach it
to another gateway, an SDK, an edge/WAF worker, or a coding-agent plugin.

## What it inspects

| Area | Catalog |
|---|---|
| Jailbreaks, toxicity, topic/keyword moderation | [Content security](/trustguard/detectors/content-security) |
| PII and secrets (optional in-flight masking) | [Data loss prevention](/trustguard/detectors/data-loss-prevention) |
| URLs and uploaded documents (indirect prompt injection, PII) | `url_analyzer`, `doc_analyzer` |
| Tool-sourced content (MCP / `role=tool`) | [Agent & MCP security](/trustguard/detectors/agent-mcp-security) |

Detectors only **find**. What happens next is the [policy](/trustguard/concepts/policies):
gates first (matching attributes such as consumer, model, tool, and source), then detector rules
(**Monitor** / **Block** / **Transform**).

## Where traffic comes from

Create a collector under **Runtime → Collectors → Catalog**.

| Group | Examples |
|---|---|
| **Gateway** | [TrustGate](/integrations/trustgate) (native; no `tgk_…` key), Portkey, LiteLLM, Kong, Apigee, Azure APIM |
| **IDE & coding agents** | [Claude Enterprise](/integrations/claude#claude-enterprise), [Claude Code](/integrations/claude#claude-code), [Cursor](/integrations/cursor), [Codex](/integrations/codex), [GitHub Copilot](/integrations/github-copilot) |
| **Agent frameworks** | [LangChain](/integrations/langchain) (`langchain-neuraltrust`), [n8n](/integrations/n8n) (`@neuraltrust/n8n-nodes-trustguard`) |
| **Application** | [Python](/integrations/python-sdk) / [Node](/integrations/node-sdk) SDK, REST, middleware |
| **Edge / WAF** | Cloudflare, CloudFront, Fastly, Akamai |

Most collectors call [`POST /v1/evaluate`](/trustguard/api/evaluate) with a collector
API key. TrustGate authenticates as the gateway. Claude Enterprise uses
`POST /v1/evaluate/claude`.

<Warning>
A collector with **no matching policy** is **unguarded**: TrustGuard returns
`status: "allow"` and inspects nothing. Assign a default policy (and per-consumer
overrides if you need them) before you rely on it.
</Warning>

## IDE & coding agents

The **IDE & coding agents** catalog group covers AI assistants and developer
tools. TrustGuard sees **prompts and tool use** (shell, MCP `tools/call`, tool
results), not only chat.

| Collector | Path | `source.application` | Enforcement |
|---|---|---|---|
| [Claude Enterprise](/integrations/claude#claude-enterprise) | Anthropic organization, server-side (`/v1/evaluate/claude`) | `claude-ai`, `claude-code` | Allow or deny only. Ask is treated as Allow because this endpoint has no interactive confirmation. |
| [Claude Code](/integrations/claude#claude-code) | Laptop plugin + binary + `tgk_…` | `claude-code-plugin` | Block on prompts; Block or Ask on tool calls |
| [Cursor](/integrations/cursor) | Plugin + MDM `cursor.json` | `cursor-plugin` | Block on prompts; Block or Ask on tool calls |
| [Codex](/integrations/codex) | Plugin / managed hooks + `codex.json` | `codex-plugin` | Block to stop. Ask is extra context only (no dialog) |
| [GitHub Copilot](/integrations/github-copilot) | Plugin / policy hooks + `copilot.json` | `copilot-plugin` | Block or Ask on tool calls; prompt events are audit-only |

A gate on `claude-code` (Claude Enterprise, server-side) does **not** match the laptop plugin
(`claude-code-plugin`). TrustGate MCP is an [organization connector](/integrations/claude),
not one of these plugins. Do not put a `tgk_…` key on the connector.

Full list: [Integrations](/integrations/overview).

## Building blocks

| Concept | What it is |
|---|---|
| **[Detector](/trustguard/concepts/detectors)** | A named instance of a [catalog](/trustguard/detectors/overview) detector plus its settings. Detection-only. |
| **[Policy](/trustguard/concepts/policies)** | Gates + detector rules (Input / Output), and **Observe / Enforce**. Built under **Runtime → Policies**. |
| **[Collector](/trustguard/concepts/collectors)** | The integration point that sends requests to a policy. |
| **Finding** | What a gate or detector reported (`source`, `signal`, `outcome`, `evidence`). |

The request does **not** name detectors. TrustGuard resolves the collector, picks the
policy, runs gates, then the detector rules that match `direction`. See
[How it works](/trustguard/how-it-works).

## Observe vs Enforce

On the policy **Basics** tab:

| Console | Effect |
|---|---|
| **Observe** | Record only. Status never goes past `report`. |
| **Enforce** | Apply **Block** / **Ask** / **Transform** as configured. |

```text
block  →  ask  →  transform  →  report  →  allow
```

The collector acts on `status`. TrustGate enforces the verdict on the route.
Coding-agent behavior varies by event and host; see
[Coverage](/integrations/coverage). A successful evaluation returns `HTTP 200`
even when `status` is `block`. A non-2xx response indicates an authentication,
request, or evaluation error.

## Get started

1. Create a [collector](/trustguard/concepts/collectors), such as TrustGate, a coding-agent
   collector, or an SDK.
2. Create [detectors](/trustguard/detectors/overview) you need (skip this if the policy
   is gates only).
3. Create a [policy](/trustguard/concepts/policies) in **Observe**. Add gates and/or
   Input / Output detector rules.
4. Attach it to the collector (**Policies** tab on the collector, or **Collectors** on
   the policy).
5. Run a sample on the policy **Test** tab, then check **Activity**.
6. Switch the policy to **Enforce** when the signal looks right.

## Where to go next

<CardGroup cols={2}>
  <Card title="How it works" icon="workflow" href="/trustguard/how-it-works">
    Collector → policy → gates → detectors → status.
  </Card>
  <Card title="Policies" icon="shield-check" href="/trustguard/concepts/policies">
    Tabs, Observe / Enforce, gates (including Ask), Test.
  </Card>
  <Card title="Coding agents" icon="laptop" href="/integrations/overview#ai-coding-agents">
    Claude Enterprise, Claude Code, Cursor, Codex, and GitHub Copilot.
  </Card>
  <Card title="Detector catalog" icon="list" href="/trustguard/detectors/overview">
    Built-in detections and settings.
  </Card>
  <Card title="Evaluate API" icon="code" href="/trustguard/api/evaluate">
    `POST /v1/evaluate` request and response.
  </Card>
  <Card title="Telemetry Alerts" icon="siren" href="/platform/alerts">
    Turn findings into alerts.
  </Card>
</CardGroup>
