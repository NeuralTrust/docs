---
title: TrustGuard
sidebarTitle: Overview
og:title: "TrustGuard: AI Guardrails for LLM and Agent Traffic"
twitter:title: "TrustGuard: AI Guardrails for LLM and Agent Traffic"
description: "Inspect prompts, responses, and tool activity at runtime. TrustGuard returns a verdict; the collector enforces it."
---

<p className="nt-lead">
  TrustGuard evaluates prompts, completions, documents, URLs, and tool calls,
  then returns a structured verdict. It is not a gateway: it does not route or
  call model providers. The collector that called it decides whether to allow,
  mask, ask, or block.
</p>

<Warning>
A collector with **no matching policy** is unguarded. TrustGuard returns
`status: "allow"` and inspects nothing. Attach a default policy before you
rely on it.
</Warning>

<CardGroup cols={2}>
  <Card title="Connect a collector" icon="plug" href="/integrations/overview">
    TrustGate, coding agents, other gateways, SDKs, or middleware.
  </Card>
  <Card title="How verdicts are enforced" icon="workflow" href="/trustguard/how-it-works">
    Block, Ask, Transform, and Monitor — and what each collector can actually apply.
  </Card>
</CardGroup>

<img
  src="/images/static/img/trustguard-product.svg"
  alt="Flow diagram showing a collector sending input or output to TrustGuard, which selects a policy, evaluates gates and detector rules, and returns a verdict for the collector to apply."
/>

## What it stops

<CardGroup cols={3}>
  <Card title="Data loss" icon="eye-off" href="/trustguard/detectors/data-loss-prevention">
    PII and secrets. Set **Transform** to mask them in flight.
  </Card>
  <Card title="Jailbreaks and abuse" icon="shield-off" href="/trustguard/detectors/content-security">
    Prompt injection, toxicity, topic controls, URLs, and documents.
  </Card>
  <Card title="Tool-borne attacks" icon="bot" href="/trustguard/detectors/agent-mcp-security">
    Indirect injection in MCP and other `role=tool` content.
  </Card>
</CardGroup>

Detectors **find** risk. The [policy](/trustguard/concepts/policies) decides what
happens next.

## Building blocks

<CardGroup cols={2}>
  <Card title="Detectors" icon="scan-search" href="/trustguard/concepts/detectors">
    A catalog detector plus its settings. Detection only — they do not enforce.
  </Card>
  <Card title="Policies" icon="file-text" href="/trustguard/concepts/policies">
    Gates, Input / Output rules, and Observe vs Enforce.
  </Card>
  <Card title="Collectors" icon="radar" href="/trustguard/concepts/collectors">
    The integration point that sends traffic to a policy.
  </Card>
  <Card title="Evaluate API" icon="code" href="/trustguard/api/evaluate">
    `POST /v1/evaluate`. Most collectors call this; TrustGate does not need a `tgk_` key.
  </Card>
</CardGroup>

## Get started

TrustGuard is entitlement-gated. If your team does not have it, **Agent Runtime**
does not appear in the console. The fastest path is **Agent Runtime → Getting
started**.

1. **Build a policy** from the [detector catalog](/trustguard/detectors/overview). Leave it in **Observe**.
2. **Test it** on the policy **Test** tab. The tab evaluates the *saved* policy.
3. **Connect a collector** under **Collectors → Catalog**, then attach the policy.

Watch **Activity**, then switch the policy to **Enforce**.

## FAQ

<AccordionGroup>
<Accordion title="Is TrustGuard an AI gateway?">
No. It evaluates traffic and returns a verdict. The collector applies it. When
that collector is TrustGate, the gateway also routes the call.
</Accordion>
<Accordion title="Do I need TrustGate?">
No. Use another supported gateway, an SDK, middleware, or a coding-agent plugin.
See [Integrations](/integrations/overview).
</Accordion>
<Accordion title="Can I evaluate a policy without blocking traffic?">
Yes. **Observe** records findings and downgrades block, ask, and transform to
`report`. Switch to **Enforce** after Activity looks right.
</Accordion>
<Accordion title="Can TrustGuard inspect tool calls?">
Yes, when the collector sends tool events. Which actions the host can apply is
in [How it works](/trustguard/how-it-works).
</Accordion>
</AccordionGroup>
